version: '3.3'
services:
    proxy:
        image: caddy:latest
        ports:
            - 80:80
            - 443:443
        
        volumes:
            - ./testing.com.pem:/etc/certs/localhost.crt
            - ./testing.com-key.pem:/etc/certs/localhost.key

    web:
        image: nginx:latest